Pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0
APScheduler>=3.10.0
moviepy>=2.0.0
streamlit>=1.30.0
praw>=7.7.0
httpx>=0.27.0

# Shorts Factory Bot
edge-tts>=6.1.0
gTTS>=2.5.0
openai>=1.0.0
google-generativeai>=0.8.0

# Optional Shorts Factory packages
# pip install faster-whisper   # word-level captions (CPU)
# pip install replicate        # cloud image gen

# Optional Memebot LLM providers
# pip install openai anthropic
# anthropic>=0.25.0

# Optional live X posting (dry-run works without this)
# tweepy>=4.14.0
